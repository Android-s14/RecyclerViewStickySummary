<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.android_s14.rvsummary.sample"
    xmlns:android="http://schemas.android.com/apk/res/android"
    >

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        >
        <activity

            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".SampleShortRecycler"
            android:label="@string/app_name"
            >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.android_s14.rvsummary.sample.MainActivity"
                />
        </activity>

        <activity
            android:name=".SampleLongRecycler"
            android:label="@string/title_activity_sample_long_recycler"
            android:parentActivityName=".MainActivity"
            >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.android_s14.rvsummary.sample.MainActivity"
                />
        </activity>
    </application>

</manifest>
